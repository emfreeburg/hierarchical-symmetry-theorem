# v3 — current repository version (arXiv replacement forthcoming)

- `v3-preprint.pdf` — the author's compilation of the v3 source
  (JSP format, 27 pp.), placed here as a **placeholder** until the
  arXiv v3 announcement; it will then be replaced by the
  arXiv-watermarked original, following the convention of `v1/`
  and `v2/`.
- The v3 source is the repository top level ([`../../paper/`](../../paper/)).
